# Round 6 抛光小结（v2.7.23·103）

日期：2026-09-01  
范围：`d:\Downloads\mandala-shichen\www`（未改 `d:\Downloads\mant`）

## 已完成

### 1. 思维导图
- **关联线点选菜单**：反转方向 / 删除（替代直接 confirm 删除）
- **关联起点高亮**（橙色描边）；起点小圆点 + ↩ 角标命中加大
- **主线/同级排序反馈**：toast「主线 第 N/M」+ 短暂闪动 + 平滑居中
- **窄屏浮动条 compact**：隐藏文字标签、横向可滑，次要按钮略淡
- **回格子跳转**：rAF 衔接 + 更短 toast；`centerOn` 短距缓动

### 2. 流式 → 上板
- 流式「可上板」条 / ready chip：**跳过 ≥3 确认**（`skipConfirm`）
- 上板后**不关对话窗**，显示「已上板 N 步」条，可继续追问

### 3. 时钟 / 记录
- **Shift+■**：已命名钟秒归档（跳过命名窗）
- 启动发现跨日钟：toast 带 **「带到今日」** 一键动作
- 记录页计划标题：**单击改标题 · 双击开钟**

## 版本信号
- `APP_BUILD`：**103**
- `sw.js`：`mandala-v103`
- `index.html`：`?v=20260901r6`

## Round 7 建议
1. 导图：拖拽改主线顺序手势；关联线标签文案
2. 待办看板虚拟列表 / 骨架；完成反馈更轻
3. 跨日钟自动归档偏好开关（设置页）
4. 便利贴多选批量移格
5. 真机回归 + 启动耗时抽样

## 本地预览
```bash
cd d:\Downloads\mandala-shichen
npm start
# http://localhost:8080 硬刷新，确认徽标 v2.7.23·103
```
