# Round 5 抛光小结（v2.7.23·102）

日期：2026-09-01  
范围：`d:\Downloads\mandala-shichen\www`（未改 `d:\Downloads\mant`）

## 已完成

### 1. 思维导图 · 关联线与主线排序
- 浮动条 **🔗 关联**：进入绘制模式，再点目标节点建立 `meta.link`（再点同一对可清除）
- Esc / 再次点关联 取消；桌面与触屏均支持
- **Alt+↑↓** 同级排序（含根下主线顺序）；**Alt+←/→** 进入关联模式
- 快捷键面板同步说明；关联按钮高亮态

### 2. 对话流式 → 可上板
- 流式回调中用 `parseIncubateItems` 识别结构（≥2 条）
- 孵化 chip 变为 **「可上板 · N」** 并脉冲；顶部出现 **「检测到 N 步 · 点此上板」**
- 流式中途点击亦可上板（写入 `_pendingIncubateText`）

### 3. 多时钟 · 带到今日选格
- 单口跨日钟「→今」打开 **时辰/格子选择器**（可「用原格」）
- `bringOneTimerToToday` 支持 `toPeriod` / `toCell`；批量仍用原格

### 4. 记录页
- 计划任务 **标题行内快编**（点标题 → prompt）

## 版本信号
- `APP_BUILD`：**102**
- `sw.js`：`mandala-v102`
- `index.html`：`?v=20260901r5`

## Round 6 建议
1. 导图：自由拖拽改主线顺序手势、关联线双向/标签、浮动条在窄屏折叠
2. 看板流式/骨架与滚动性能；待办完成反馈更轻
3. 多钟：跨日自动归档选项；命名窗更快确认
4. 记录/待办：批量便利贴；备注富文本精简
5. 真机回归全量 + 启动/渲染耗时抽样

## 本地预览
```bash
cd d:\Downloads\mandala-shichen
npm start
# http://localhost:8080 硬刷新，确认徽标 v2.7.23·102
```
