# Round 11 抛光小结（v2.7.24·109）

日期：2026-09-02  
范围：`d:\Downloads\mandala-shichen`（未改 `d:\Downloads\mant`）  
承接：Round10「仍可继续」+ 用户反馈：侧条别做成左隐藏整页、记录多钟、导图安卓触控

---

## 一、侧条方案理由（关键）

| 问题 | 旧方案 | Round11 方案 |
|------|--------|--------------|
| 形态 | 左缘滑出 **82vw 全高抽屉**，像「左隐藏页面」 | **主九宫格贴边浮动轨**（`edge-rail`），窄条半透明浮层 |
| 遮罩 | 深色全屏 mask，占住整页交互感 | 透明 mask，仅点外部收起；拖贴格时半收起露出时辰格 |
| 标签 | 大号 pill，占纵向空间多 | **更小标签 chips**（约 11px / 紧凑 padding） |
| 路径 | 选标签 → 任务 → 贴格 / 长按拖入 | 保留并强化：选标签加宽轨 → 任务 → **拖/点贴时辰格** |
| 左侧页 | 抽屉占用「左侧整页」心智 | **腾出左侧页面给未来**；边轨只是贴格工具，不是页面 |

本质：侧条是「标签 → 任务 → 落到时辰格」的快捷轨，不是导航页。

---

## 二、记录页 · 多钟

| 子维度 | 状态 | 说明 |
|--------|------|------|
| 内嵌多钟条 | ✅ | `#recordClockStrip`：在跑/暂停汇总 + chip |
| 启动 / 暂停 / 恢复 | ✅ | 格子锚点 + 条内 ▶/⏸ |
| 切下一钟 | ✅ | ⏸ 后 toast「开始下一钟」；条内 ⏭ |
| 结束·命名 | ✅ | ■ → 命名归档弹窗；Shift+■ 秒归档 |
| 时长文案 | ✅ | 修复 `spentMin` 未定义；用 `formatSpentPrecise` |
| 底栏同源 | ✅ | `renderRunningTimerBar` 同步刷新记录页条 |

---

## 三、思维导图 · 安卓优先 + 手动调整

| 子维度 | 状态 | 说明 |
|--------|------|------|
| 点选 | ✅ | 短按选中 + 浮动条 / 节点面板 |
| 长按拖 | ✅ | ≥280ms 武装后拖：摆放 / 同级改序 / 异级改隶属 |
| 未长按滑动 | ✅ | 转为平移画布，减少误拖 |
| 双击改标题 | ✅ | 保留 |
| 节点触控区 | ✅ | 最小高度 48px |
| 浮动条 | ✅ | ≥44px 命中，触控可点 |
| 任务节点 | ✅ | 可拖移；↩ 跳格子；浮动条改标题/增删/升降级 |

---

## 四、版本信号

- `APP_BUILD`：**109**
- `APP_VERSION`：**2.7.24**
- `sw.js`：`mandala-v109`
- `index.html`：`?v=20260902r11`
- Android：`versionCode 109` / `versionName 2.7.24.109`

---

## 仍可继续（Round12 候选，非阻塞）

1. 真机 APK：边轨贴格 + 记录多钟 + 导图长按拖
2. 侧条「一键贴本页全部待安排」批量
3. 导图关联文案在大纲/导出文本中汇总
4. 边轨在计划页九宫格内相对定位（进一步贴格感）
5. 记录页多钟条与底栏在窄屏的避让

## 本地预览

```bash
cd d:\Downloads\mandala-shichen
npm start
# http://localhost:8080 硬刷新，确认徽标 v2.7.24·109
```

## 关键文件

- `www/app.js` — 边轨逻辑、多钟条、导图长按、版本 109
- `www/styles.css` — 浮动边轨 + 记录多钟条 + 小标签
- `www/index.html` — `edge-rail`、`recordClockStrip`、cache bust `r11`
- `www/sw.js` — `mandala-v109`
- `android/app/build.gradle` — versionCode 109
- `ROUND11.md` — 本文件
