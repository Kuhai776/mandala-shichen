# Round 7 抛光小结（v2.7.24·105）

日期：2026-09-02  
范围：`d:\Downloads\mandala-shichen\www`（未改 `d:\Downloads\mant`）  
说明：v104 完成主干后代理中断；本版（105）补齐侧条拖入样式、记录密度、中文文案、安卓画板空间与文档子维度。

---

## 一、计划页 · 去掉正计时

| 子维度 | 状态 | 说明 |
|--------|------|------|
| 正计时锚点 | ✅ | 计划九宫格不再渲染 ⏱/⏸/■ |
| 计时描边 | ✅ | 不再加 `timing` / `timing-paused` |
| 记录页保留 | ✅ | 记录页锚点、多钟条、命名归档原样保留 |
| 规划专注 | ✅ | 计划页回归「只做规划」 |

---

## 二、主页侧条 · 拖任务到时辰格

| 子维度 | 状态 | 说明 |
|--------|------|------|
| 半透明圆形条 | ✅ | 左缘把手圆形长条；标签为半透明胶囊 pill |
| 选标签→任务 | ✅ | 点标签折叠列表，只看该标签任务 |
| 层级展示 | ✅ | 侧条任务显示「主线·/支线·/子步」 |
| 拖入时辰格 | ✅ | 与待办同款：拖到格子安排/移动 |
| 拖入提示 | ✅ | 「⋮⋮ 拖入格子」+ toast「开始计时」（记录页） |
| 非整页抢占 | ✅ | 抽屉半透明毛玻璃，不占独立导航页 |

---

## 三、记录页 · 条状 / 精确时长 / 多钟 / 层级 / 自适应

| 子维度 | 状态 | 说明 |
|--------|------|------|
| 条状任务 | ✅ | 计划任务与记录实际内容均为条/芯片，非整段串联 |
| 精确时长 | ✅ | `spent` 写到秒，文案「分/秒」（兼容旧 min/s） |
| 暂停→下一钟 | ✅ | 暂停后 toast「开始下一钟」可一键续跑，或忽略保持全停 |
| 层级展示 | ✅ | 格子任务徽标：主线 / 支线 / 子步 |
| 多任务自适应 | ✅ | `dense-2/3/4` 自动缩小字号与间距 |

---

## 四、思维导图 · 深抛光 / 导出导入 / 安卓 / 画板空间

| 子维度 | 状态 | 说明 |
|--------|------|------|
| 层级语义 | ✅ | 角标「主线 / 支线 / 子步」；提升/降级按根→主线→支线→子步定 type |
| 浮动条 | ✅ | 命中 ≥44px；先量宽再定位；含「降级」 |
| 拖拽 | ✅ | hit-test 前藏 ghost；松在浮动条不当空白；12px 网格吸附 |
| 上板动效 | ✅ | 入场轻微上浮 `--mm-rise` |
| 可上板 | ✅ | 流式 ≥1 条结构即点亮 |
| 导出/导入 | ✅ | 「导出数据 / 导入数据」往返；下载 append 兼容 WebView |
| 安卓触控 | ✅ | 工具条横滑、按钮加大、安全区留白 |
| 画板空间 | ✅ | 点阵弱化；图例/统计半透明缩小；窄屏工具条单行不占双层大块 |

---

## 五、界面语言 · 全中文实用向

| 子维度 | 状态 | 说明 |
|--------|------|------|
| 导图工具栏 | ✅ | 图片 / 流程图 / 大纲文本 / 网页 / 导出数据 / 导入数据 |
| 提示与 toast | ✅ | 导图相关成功/失败提示改为中文 |
| 时长单位 | ✅ | 「分」「秒」替代 min/s（解析仍兼容旧格式） |
| 标签用语 | ✅ | 侧条/导图「标签」；避免面向用户的 JSON/PNG/TAG 缩写 |
| 整体调性 | ✅ | 现代实用；不堆英文缩写角标 |

---

## 六、版本信号

- `APP_BUILD`：**105**
- `APP_VERSION`：**2.7.24**
- `sw.js`：`mandala-v105`
- `index.html`：`?v=20260902r7b`
- Android：`versionCode 105` / `versionName 2.7.24.105`

---

## 仍可继续（非阻塞）

1. 原生 APK 用 Capacitor Share / Filesystem 替代 `<a download>`（部分机型仍可能拦下载）
2. 导图关联线标签文案 / 拖拽改主线顺序手势
3. 触屏侧条长按拖（目前 HTML5 DnD 桌面为主；安卓 WebView 多数可用）
4. 真机回归：计划无钟、记录有钟、侧条拖入、数据往返、窄屏画板

## 本地预览

```bash
cd d:\Downloads\mandala-shichen
npm start
# http://localhost:8080 硬刷新，确认徽标 v2.7.24·105
```

## 关键文件

- `www/app.js` — 计划去钟、侧条拖入、记录条状/精确时长/下一钟、导图 IO、中文文案、版本
- `www/index.html` — 中文导出按钮、侧条标题、cache bust
- `www/styles.css` — 密度/层级/条状芯片、侧条玻璃、安卓画板空间
- `www/sw.js` — `mandala-v105`
- `android/app/build.gradle` — versionCode 105
